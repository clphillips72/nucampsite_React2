// Set the value of baseUrl to the network address of where we're running the JSON server.  Now we can use this const wherever in our code that we need to get the server address.  If the 
// address ever changes, we only need to update it once here.
export const baseUrl = 'http://localhost:3001/';   